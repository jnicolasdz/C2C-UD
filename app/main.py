"""Initializes the FastApi application, including:
        -creating the app instance
        -registering the API routers
        -attaching the database session to the app state (middleware)
        -loading environment variables from a .env file (aplication settings)
        -configuring CORS settings for cross-origin requests
"""
